name: currencies_viewer_test
description: Application for viewing currencies.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  hive: ^2.0.4
  hive_flutter: ^1.1.0
  cupertino_icons: ^1.0.2
  flutter_bloc: ^7.3.1
  connectivity: ^3.0.6
  animations: ^2.0.1
  http: ^0.13.4
  intl: ^0.17.0
  path_provider: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^1.1.1
  build_runner: ^2.1.4

  flutter_lints: ^1.0.0


flutter:

  uses-material-design: true
